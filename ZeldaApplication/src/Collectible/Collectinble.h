#pragma once

#include <Engine.h>
#include "../Entity/Entity.h"

enum collectibles
{
	C_KEY,
	C_REDPOTION,
	C_BLUEPOTION,
	C_LINK,
	C_POINTS
};

class Collectible : public Entity
{
public:

private:

};
